# Runtime data — do not backup
agents/
logs/
media/
canvas/
delivery-queue/
*.bak
*.bak.*
*.clobbered*

# OS noise
.DS_Store
Thumbs.db

# Credentials (push separately if needed)
credentials/

# SQLite databases with secrets
memory/*.sqlite
tasks/runs.sqlite*
flows/registry.sqlite*
openclaw.json
openclaw.json.last-good
# SQLite temp files
*.sqlite-shm
*.sqlite-wal
*.sqlite-shm
*.sqlite-wal

# Cron run logs
cron/runs/
